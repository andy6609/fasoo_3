# Local Proxy 기반 확장 서비스 아이디어

## 1. 업로드 직전 자동 암호화 서비스

사용자가 외부로 파일을 올리려고 할 때 프록시가 전송을 잠시 보류하고 파일 내용을 검사한다.

```text
파일 업로드 시도
   ↓
프록시가 파일 전체 수신
   ↓
본문 분석
   ↓
민감 문서로 판단
   ↓
암호화된 파일 생성
   ↓
원본 대신 암호화 파일 업로드
```

예를 들어 `customer_list.xlsx`에서 주민등록번호, 전화번호, 고객명 등 민감정보가 발견되면 원본 업로드를 중단하고 암호화된 사본만 전송한다.

이 기능은 필요한 순간에만 암호화하는 **Just-in-Time Encryption** 방식으로 정의할 수 있다.

적용 가능한 암호화 방식은 다음과 같다.

- 비밀번호 기반 ZIP 또는 문서 암호화
- 회사 공개키 기반 암호화
- 수신자 인증서를 이용한 암호화
- AES 파일 암호화 후 키 별도 전달
- 웹 뷰어에서만 열 수 있는 보안 컨테이너
- 만료일과 열람 횟수가 적용된 보호 파일

외부 서버로 파일이 전달된 뒤 암호화하는 것은 늦기 때문에 다음과 같은 **Store-and-Forward 구조**가 필요하다.

```text
브라우저
   ↓
프록시 임시 저장
   ↓
검사 및 변환
   ↓
외부 서버
```

---

## 2. 안전한 링크 자동 전환 서비스

위험한 파일을 이메일이나 클라우드에 직접 첨부하지 않고 회사의 보안 저장소로 자동 전환하는 방식이다.

```text
사용자가 웹메일에 기밀파일 첨부
   ↓
프록시가 위험 파일로 판단
   ↓
원본 첨부 차단
   ↓
파일을 회사 보안 저장소에 업로드
   ↓
만료되는 다운로드 링크 생성
   ↓
메일에는 링크만 삽입
```

수신자는 다음 조건을 충족할 때만 파일을 열람할 수 있다.

- 이메일 인증
- 일회용 비밀번호
- 지정된 회사 도메인
- 특정 IP 대역
- 최대 열람 횟수
- 다운로드 금지
- 일정 기간 후 자동 만료
- 화면 워터마크 표시

이 방식은 모든 문서를 항상 암호화하지 않으면서 외부 전달이 필요한 중요 파일만 보호할 수 있다.

---

## 3. 위험 문서 자동 추적·보호 서비스

위험한 파일이 업로드될 때 파일의 SHA-256뿐 아니라 내용 기반 지문을 생성한다.

```text
원본 파일
   ↓
파일 해시
본문 텍스트 지문
문단별 해시
표·시트별 지문
이미지 특징값
   ↓
문서 식별자 생성
```

이후 파일명이 변경되거나 일부 내용이 수정되어도 같은 문서 계열인지 탐지한다.

예시는 다음과 같다.

```text
secret.xlsx
secret_final.xlsx
report.xlsx
secret.zip 안의 report.xlsx
PDF로 변환된 secret.pdf
일부 내용이 복사된 new_document.docx
```

이 파일들을 하나의 데이터 계보로 연결한다.

```text
원본 문서 A
 ├─ PDF 변환본
 ├─ 이메일 첨부본
 ├─ AI 업로드본
 └─ 일부 내용이 복사된 파생 문서
```

이 기능은 **Data Lineage** 또는 **Document Lineage Tracking** 서비스로 정의할 수 있다.

로컬 파일을 추적하려면 Endpoint Agent가 동일한 해시 또는 내용 지문을 가진 파일을 찾아 자동 분류하거나 암호화한다.

```text
업로드된 파일 해시 확인
   ↓
Endpoint Agent가 동일·유사 파일 검색
   ↓
파일 자동 분류
   ↓
암호화 또는 접근 권한 적용
```

회사에서 관리하는 클라우드라면 서비스 API를 통해 다음 조치를 수행할 수 있다.

- 공유 링크 제거
- 외부 사용자 권한 회수
- 파일 삭제
- 격리 폴더로 이동
- 보안 라벨 적용
- 관리자 소유권으로 변경
- 암호화된 사본으로 교체

---

## 4. AI 안전 변환 서비스

AI로 보내려는 문서가 위험하면 전체 업로드를 차단하는 대신 민감정보를 제거한 안전 사본을 생성한다.

```text
원본 문서
   ↓
본문 추출
   ↓
개인정보·영업비밀 탐지
   ↓
마스킹 또는 가명화
   ↓
안전 사본 생성
   ↓
AI에 안전 사본 업로드
```

예를 들어 다음 원문이 있다고 가정한다.

```text
홍길동 고객의 주민번호는 900101-1234567이고,
담당자는 김민수 과장이다.
```

AI에는 다음과 같이 변환해 전송한다.

```text
[CUSTOMER_001] 고객의 주민번호는 [RRN_001]이고,
담당자는 [EMPLOYEE_001]이다.
```

AI가 문서를 요약하거나 수정한 뒤 결과가 돌아오면, 권한이 있는 사용자에게만 로컬에서 원래 값을 복원할 수 있다.

```text
원본 데이터
   ↓
로컬 토큰화
   ↓
AI에는 가명 데이터 전송
   ↓
AI 응답 수신
   ↓
권한 있는 사용자 화면에서 복원
```

---

## 5. 자동 외부 배포본 생성 서비스

기밀 문서가 외부로 나갈 때 원본을 차단하고 외부 배포용 파일을 자동 생성한다.

```text
원본 DOCX
   ↓
민감정보 삭제
문서 속성 제거
작성자 정보 제거
숨겨진 시트 제거
주석 제거
변경 이력 제거
수신자 워터마크 삽입
PDF 변환
   ↓
외부 배포본 생성
   ↓
배포본만 전송
```

예를 들어 Excel 파일에 숨겨진 시트, 내부 단가, 고객 목록, 작성자 메타데이터가 포함되어 있다면 다음과 같이 변환한다.

```text
원본
- 숨겨진 시트 존재
- 내부 단가 포함
- 고객 목록 포함
- 작성자 메타데이터 포함

외부 배포본
- 허용된 시트만 포함
- 내부 단가 삭제
- 고객명 가명화
- 메타데이터 제거
- 수신자용 워터마크 삽입
```

사용자가 직접 사본을 만들고 민감정보를 지우지 않아도 제품이 안전한 외부 배포본을 자동 생성한다.

---

## 6. 자동 승인 서비스

위험 파일을 무조건 차단하지 않고 위험도에 따라 승인 절차를 자동화한다.

```text
파일 분석
   ↓
위험 점수 계산
   ↓
정책 실행
```

예시는 다음과 같다.

```text
0~20점    자동 허용
21~50점   사용자 경고 후 허용
51~70점   팀장 승인
71~90점   보안팀 승인
91점 이상 즉시 차단
```

위험 점수에는 다음 정보를 활용할 수 있다.

- 개인정보 개수
- 문서 기밀 키워드
- 문서 크기
- 외부 목적지
- 개인 계정 여부
- 사용자의 부서
- 근무 시간 외 전송
- 대량 파일 전송
- 압축·암호화 여부
- 이전 차단 이력
- 처음 사용하는 사이트
- 비정상적인 업로드 빈도

승인 화면에는 파일 전체 원문 대신 다음과 같은 요약 정보를 제공한다.

```text
파일명: 2026_customer_list.xlsx
대상: 개인 Gmail
탐지: 주민번호 54건, 전화번호 120건
사용자 사유: 외부 세무법인 전달
추천 조치: 보안 링크로 변환 후 허용
```

---

## 7. 실시간 사용자 코칭 서비스

위험한 업로드가 발생했을 때 단순히 차단하지 않고 사용자가 문제를 해결할 수 있도록 안내한다.

```text
현재 파일은 업로드할 수 없습니다.

탐지 내용:
- 주민등록번호 3건
- 내부 프로젝트 코드명 2건

권장 조치:
[민감정보 자동 제거]
[보안 링크로 전환]
[승인 요청]
[업로드 취소]
```

사용자가 **민감정보 자동 제거**를 선택하면 제품이 안전한 사본을 생성하고 업로드를 계속한다.

이 기능은 보안 제품을 단순한 차단 도구가 아니라 사용자의 안전한 업무를 지원하는 서비스로 바꿀 수 있다.

---

## 8. 업로드 후 자동 회수 서비스

파일이 외부로 전송된 직후 정밀 분석에서 위험한 파일로 판단되면 가능한 범위에서 자동 회수한다.

```text
파일 전송 완료
   ↓
정밀 분석 결과 위험 판단
   ↓
전송 대상 서비스 API 호출
   ↓
파일 삭제·권한 회수·링크 만료
```

가능한 조치는 다음과 같다.

- Slack 메시지와 첨부파일 삭제
- Teams 파일 공유 권한 해제
- OneDrive 공유 링크 취소
- Google Drive 외부 공유 제거
- 회사 GitHub 저장소에서 Secret 포함 작업 차단
- 웹메일 발송 취소 기능 호출
- 보안 담당자에게 긴급 알림

다만 외부 서버에 삭제·회수 API가 없거나 관리 권한이 없다면 이미 업로드된 파일을 회수할 수 없다.

따라서 빠른 검사는 업로드 전에 실행하고, 업로드 후 자동 회수는 보조 기능으로 사용한다.

---

## 9. 문서 자동 보호 등급 서비스

업로드되는 문서의 내용을 분석하여 제품이 자동으로 보안 등급을 부여한다.

```text
파일 최초 외부 전송 시도
   ↓
본문 분석
   ↓
문서 등급 결정
```

예시는 다음과 같다.

```text
공개
- 홈페이지 자료
- 공개 제품 설명서

내부
- 회의록
- 일반 업무자료

기밀
- 고객정보
- 계약조건
- 내부 가격

극비
- 핵심 소스코드
- 인수합병 자료
- 인증키
- 미공개 설계자료
```

등급이 결정되면 다음 조치를 수행할 수 있다.

- Windows 파일 속성에 등급 저장
- NTFS Alternate Data Stream에 태그 저장
- 중앙 데이터베이스에 파일 해시와 등급 등록
- 문서 메타데이터에 등급 삽입
- 파일명 또는 워터마크에 등급 표시
- 고위험 파일 자동 암호화
- 복사·USB·출력 제한

사용자가 직접 문서의 보안 등급을 지정하지 않아도 되는 자동 분류 서비스로 발전시킬 수 있다.

---

## 10. 내부자 위험 탐지 서비스

개별 파일만 검사하지 않고 사용자의 업로드 행동을 시간에 따라 분석한다.

예를 들어 다음과 같은 행동은 내부자 위험 신호가 될 수 있다.

```text
평소 하루 1~2개 업로드
   ↓
퇴사 예정일 직전
   ↓
새벽 시간에 500개 파일 업로드
   ↓
개인 Google Drive 사용
```

탐지 대상은 다음과 같다.

- 짧은 시간에 대량 업로드
- 압축 후 업로드
- 파일 확장자 변경
- 개인 메일 반복 전송
- 여러 AI 사이트에 같은 문서 업로드
- 근무 시간 외 전송
- 평소 사용하지 않던 서비스 접속
- 업로드 차단 후 다른 서비스로 재시도
- 같은 문서를 여러 형식으로 변환
- 파일을 작은 조각으로 나누어 전송

위험도가 높아지면 다음 조치를 자동 적용할 수 있다.

```text
위험 점수 상승
   ↓
업로드 제한 강화
   ↓
사용자 재인증
   ↓
관리자 승인 요구
   ↓
네트워크 세션 일시 중지
   ↓
보안팀 알림
```

---

## 11. 유출 문서 식별 서비스

사내에서 외부로 전송되는 파일마다 식별 정보를 등록하고, 이후 외부에서 발견된 문서가 사내 유출 문서인지 확인한다.

등록할 수 있는 정보는 다음과 같다.

- 파일 해시
- 본문 지문
- 문단별 지문
- 이미지 지문
- 워터마크 ID
- 전송 사용자
- 전송 시각
- 수신 대상

외부에서 문서가 발견되면 다음과 같이 비교한다.

```text
발견 문서
   ↓
내용 지문 생성
   ↓
사내 문서 지문 데이터베이스와 비교
   ↓
원본 문서와 전송 경로 식별
```

사용자 또는 수신자별로 서로 다른 워터마크를 삽입하면 유출한 사용자나 최초 수신자를 추정할 수도 있다.

이 서비스는 단순한 전송 차단을 넘어 실제 유출 사고 조사와 책임 추적에 활용할 수 있다.
